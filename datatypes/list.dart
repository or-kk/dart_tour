void main() {
  var isAdd = true;
  var numbers = [
    1,
    2,
    3,
    4,
    if (isAdd) 10,
    6,
  ]; //collection if
  // numbers.add('v'); // error
  // numbers.add(5);

  print(numbers.first);
  print(numbers.last);
}
